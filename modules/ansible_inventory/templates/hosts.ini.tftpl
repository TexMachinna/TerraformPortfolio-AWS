[web]
%{ for index, ip in public_ips ~}
${environment}-web-${index + 1} ansible_host=${ip}
%{ endfor ~}

[${environment}:children]
web

[${environment}:vars]
ansible_ssh_common_args='-o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null'